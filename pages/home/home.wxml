<!--pages/home.wxml-->
<view class='container {{isShowBox?"blur__mask":""}}'>
  <view class="home__header">
    <swiperItem class="w__100"
      swiperimage="{{swiper}}"
    ></swiperItem>
    <!-- <image src='//file.yinxinlife.com/images/home_bg_header.png'></image> -->
  </view>
  <search class="comp__search" isHome="{{true}}" bindclearKeyword="clearKeyword" bindgetKeyword="goSearch"></search>
  <view class="home__tabs">
    <view class="home__tab {{currentTab == 0 ? 'isActive' :''}}" data-index = "0" bindtap = "changeTab">
      今日活动
    </view>
    <view class="home__tab {{currentTab == 1 ? 'isActive' :''}}" data-index = "1" bindtap = "changeTab">
      明日预告
    </view>
  </view>
  <view class="home__activity__group" wx:if="{{currentTab==0}}">
    <activityBlock wx:for="{{today}}" wx:key="{{index}}"
      timeStatus="{{item.time_status}}"
      startTime="{{item.starttime}}"
      endTime="{{item.endtime}}"
      price="{{item.price}}"
      realPrice="{{item.price}}"
      participateNum="{{item.part_no}}"
      activityDesc="{{item.name}}"
      cover="{{item.image}}"
      type="{{item.type}}"
      activityId="{{item.id}}"
      type_name="{{item.type_name}}"
      ></activityBlock>
      <view class="no-data" wx:if="{{today.length==0}}">
        <view class="no-data__top"><image src="../../images/icon_empty.svg"></image></view>
        <view class="no-data__bottom">暂无数据</view>
      </view>
  </view>
  <view class="home__activity__group" wx:if="{{currentTab==1}}">
    <activityBlock wx:for="{{tomorrow}}" wx:key="{{index}}"
      timeStatus="{{item.time_status}}"
      startTime="{{item.starttime}}"
      endTime="{{item.endtime}}"
      price="{{item.price}}"
      realPrice="{{item.price}}"
      participateNum="{{item.part_no}}"
      activityDesc="{{item.name}}"
      cover="{{item.image}}"
      type="{{item.type}}"
      activityId="{{item.id}}"
      type_name="{{item.type_name}}"
      ></activityBlock>
      <view class="no-data" wx:if="{{tomorrow.length==0}}">
        <view class="no-data__top"><image src="../../images/icon_empty.svg"></image></view>
        <view class="no-data__bottom">暂无数据</view>
      </view>
  </view>
</view>
<authBox isShowBox="{{isShowBox}}" bindcloseAuth="closeBox"></authBox>